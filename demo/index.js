import React from 'react';
import ReactDOM from 'react-dom';

import { Merkaba } from '../src/index.js';

const root = document.getElementById('app');

const merkaba = ReactDOM.render(<Merkaba />, root);
